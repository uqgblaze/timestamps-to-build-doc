# timestamps-to-build-doc
Python GUI tool that auto-detects a lecture timestamps table (*-timestamps.md) and VTT transcript in a folder, then generates a Markdown build document with each slide title as a heading and the matching spoken script as the body. Optionally inserts the same content into PowerPoint speaker notes.
